export type SocketNotification = {
  Timestamp: string;
  UserID: string;
  UserName: string;
  DocumentID: string;
  DocumentTitle: string;
}

export type Notification = {
  timestamp: string;
  userId: string;
  username: string;
  documentId: string;
  documentTitle: string;
}